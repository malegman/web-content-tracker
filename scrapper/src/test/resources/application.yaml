app:
  test: "scrapper_test"
  scheduler:
    interval: 5000
springdoc:
  swagger-ui:
    path: /swagger-ui
spring:
  config:
    import:
      - file:///${user.home}/.config/web-content-tracker/scrapper/application.yaml
